---
import { twMerge } from 'tailwind-merge'

interface Props {
  className?: string
}

const { className } = Astro.props
---

<section class={twMerge('max-w-4xl mx-auto px-6 mb-20', className)}>
  <slot />
</section>
